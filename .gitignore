/target
# Cargo.lock IS committed: this orchestration crate wires ~15 readers, so CI needs a
# fixed graph for deterministic `cargo deny` / `cargo vet --locked` (matching the
# audited exemption set). Downstreams still resolve their own.
Cargo.lock.orig

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# cargo-fuzz artifacts
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
